<configuration>

    <property name="stdoutEncoderPattern" value="%d{HH:mm:ss.SSS} %logger{36} - %msg%n" />
    <property name="fileEncoderPattern" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} %X{akkaSource} - %msg%n" />

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${stdoutEncoderPattern}</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>etc-client.log</file>
        <append>true</append>
        <encoder>
            <pattern>${fileEncoderPattern}</pattern>
        </encoder>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE" />
    </root>

    <logger name="io.iohk.ethereum.network.rlpx.RLPxConnectionHandler" level="ERROR" />
    <logger name="io.iohk.ethereum.network.FastSyncController" level="INFO" />
    <logger name="io.iohk.ethereum.network.PeerActor" level="ERROR" />
    <logger name="akka.io.SelectionHandler" level="ERROR" />
    <logger name="akka.io.TcpOutgoingConnection" level="ERROR" />

</configuration>
