# Default port at which mantis listen for rpc connections
mantisUrl="http://localhost:8546/"

# Used to sign transaction (must be the same as mantis/conf/storage.conf)
privatenetDatadir=${HOME}"/.privateNetTest"
keystoreDir=${privatenetDatadir}"/keystore"
